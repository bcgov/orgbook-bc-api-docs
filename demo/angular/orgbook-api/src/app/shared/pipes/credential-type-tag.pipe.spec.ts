import { CredentialTypeTagPipe } from './credential-type-tag.pipe';

describe('CredentialTypeTagPipe', () => {
  it('create an instance', () => {
    const pipe = new CredentialTypeTagPipe();
    expect(pipe).toBeTruthy();
  });
});
