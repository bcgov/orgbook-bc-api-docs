import { CredentialAttributeTagPipe } from './credential-attribute-tag.pipe';

describe('CredentialAttributeTagPipe', () => {
  it('create an instance', () => {
    const pipe = new CredentialAttributeTagPipe();
    expect(pipe).toBeTruthy();
  });
});
