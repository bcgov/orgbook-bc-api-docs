import { CredentialAttributeValuePipe } from './credential-attribute-value.pipe';

describe('CredentialAttributeValuePipe', () => {
  it('create an instance', () => {
    const pipe = new CredentialAttributeValuePipe();
    expect(pipe).toBeTruthy();
  });
});
